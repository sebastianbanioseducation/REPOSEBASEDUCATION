name: Deploy

on:
  push:
    branches: [main]

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v3

      - name: Install frontend
        run: |
          cd frontend
          npm install
          npm run build

      - name: Install backend
        run: |
          cd backend
          npm install